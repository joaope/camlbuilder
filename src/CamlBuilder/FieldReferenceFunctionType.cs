namespace CamlBuilder
{
    public enum FieldReferenceFunctionType
    {
        Average,
        Count,
        Maximum,
        Minimum,
        Sum,
        StandardDeviation,
        Variance
    }
}